# v0.8 Detailed Implementation Plan

Chinese mirror: `implementation-task-plan.zh.md`.

This milestone executes seven tasks: documentation/routing, v0.9 public surface
discovery, evidence manifest/artifact index, named artifacts/redaction scan,
bounded runtime UI/artifact display, E2E/checker handoff, and broad validation.

Each task must be implemented with task-scoped verification, recorded in
`review.zh.md`, and committed before the next task begins.
