# Scenario Operation Matrix

Chinese mirror: `scenario-operation-matrix.zh.md`.

The authoritative scenario matrix is maintained in the Chinese mirror. The key
rule is that every scenario must bind visible client operations, direct API
harvest, required artifacts, and PASS authority separately. Direct API harvest
goes to `api-log.jsonl`, not to user-visible `operation-log.jsonl`.
